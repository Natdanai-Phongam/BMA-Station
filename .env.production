# Production build — no VITE_MOCK_DATE set.
# The app uses the real browser clock (new Date()) so dates are always current.
# When connected to a real backend, patient data will match whatever period
# the user selects.
